
const ShortTitle = movies.filter(movies => movies.title.length < 9);
console.log(ShortTitle);
const LongTitle = movies.filter (movies => movies.title.length > 10);
console.log(LongTitle);
const NumberOfMovies = movies.filter(movie => movies.year >= 1980 && movie.year <= 1989);
console.log(NumberOfMovies);
let MovesRating = movies.map (function(movie){
    if (movies.rating > 7){
return movies.tag = "good"
    } 
    else (movies.rating <4) 
    return movie.tag = "bad" 
});
console.log(MovesRating);
const moviesArray = movies.filter(movies => movies.rating > 6).map(movies => movies.rating);
console.log(moviesArray);
let numderContain = movies.filter 
for (i=0; i<movies; i++){
    if (movie.title.includes("Surfer") ||movie.title.includes("Alien") || movie.title.includes("Benjamin"));


      return  
    } 
console.log(numberContain);
