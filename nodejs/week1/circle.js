class circle {
    constructor (redius) {
        this.redius = redius
    }
    getDiameter () {
        return this.radius*2;
    }
    getCircumstance (){
        return this.circumstance = 2*3.14*radius;
    }
    getArea () {
        return this.area = 3.14*Math.pow (this.radius*2);
    }
 };
 const circle = new Circle(10);
const diameter = circle.getDiameter(); // 20
    
